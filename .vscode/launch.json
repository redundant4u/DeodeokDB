{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "lldb",
      "request": "launch",
      "program": "main",
      "args": ["test.db"],
      "cwd": "${workspaceRoot}",
      "preLaunchTask": "C: Build"
    }
  ]
}
